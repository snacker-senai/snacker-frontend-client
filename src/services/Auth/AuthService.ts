// import { api } from "../api"

export class AuthService {
    static async getCurrentUser () {
        // const { data } = await api.get('/usuario/logado')
    
        // return data

        return {
            table: '1'
        }
      }
}